#include <stdio.h>

/**
 * main - Entry point
 * Description: 'This program print to STDout using printf'
 * Return: Always 0 (Success)
 */

int main(void)
{
char mystr[] = "with proper grammar, but the outcome is a piece of art,\n";
printf("%s",mystr);
return (0);
}
