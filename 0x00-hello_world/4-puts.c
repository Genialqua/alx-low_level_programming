#include <stdio.h>

/**
 * main - Entry point into the program
 * Description: 'This program prints to stdout'
 * Return: Always 0 (Success)
 */

int main(void)
{
char mystr[] = "\"Programming is like building a multilingual puzzle";

puts(mystr);
return (0);
}
