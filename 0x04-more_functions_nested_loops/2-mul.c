#include "main.h"

/**
 * mul - Multiplies two digits
 * @a: First integer as operand
 * @b: Second integer as operand
 * Return: 1 if its successful, 0 otherwise
 */

int mul(int a, int b)
{
return (a * b);
}
