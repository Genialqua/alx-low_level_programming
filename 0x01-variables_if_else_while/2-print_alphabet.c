#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Description: 'To print lowercase alphabet'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{ putchar(ch); }
	{ printf("\n"); }
	return (0);
}
