#include "main.h"

/**
 * _isalpha - Prints alphabets
 * @c: Description
 * Return: Always return 0
 */

int _isalpha(int c)
{
	int isalpha(int c);

	if (isalpha(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
